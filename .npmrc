# Refuse to install package versions younger than 7 days —
# npm malware is typically detected and pulled within a few days of publish.
# Value in minutes: 7 * 24 * 60 = 10080. Requires npm >= 11.5.
minimum-release-age=10080
